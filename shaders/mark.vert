precision mediump float;attribute vec2 a_textureCoordinates;uniform sampler2D u_positionTexture;uniform vec3 u_gridResolution;uniform vec3 u_gridSize;uniform float pointSize;uniform float positionX;uniform float positionY;vec4 a(vec2 b,float c,float d){return vec4(b*2.0-1.0,c,d);}void main(){gl_PointSize=pointSize;vec3 e=texture2D(u_positionTexture,a_textureCoordinates).rgb;e=(e/u_gridSize)*u_gridResolution;vec3 f=floor(e);vec2 g=vec2(f.z*u_gridResolution.x+f.x+0.5,f.y+0.5)/vec2(u_gridResolution.x*u_gridResolution.z,u_gridResolution.y);gl_Position=a(g,positionX,positionY);}